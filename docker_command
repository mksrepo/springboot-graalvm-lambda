# build the container locally
docker build \
  --platform=linux/arm64 \
  -f Dockerfile.native \
  -t springboot-graalvm-lambda:latest .
# run locally (port mapping if app listens on a port)
docker run -p 8080:8080 my-spring-native:latest
# test
curl http://localhost:8080/hello