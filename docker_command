docker build -t my-native-app:latest .

docker create --name temp my-native-app:latest

docker cp temp:/var/task/bootstrap ./bootstrap

file springboot-graalvm-lambda

chmod +x bootstrap

zip lambda.zip bootstrap